set -v
#
sudo apt -y update
sudo apt -y full-upgrade
sudo apt -y autoremove
sudo apt -y autoclean
# 
# dev tools
sudo apt -y install build-essential git cmake cmake-curses-gui autoconf libtool  
sudo apt -y install g++ clang nasm gdb binutils-dev 
sudo apt -y install pkg-conf
#
# utils
sudo apt -y install htop psensor gparted gedit
#
# tbb
sudo apt -y install libtbb-dev  
#
# X11
sudo apt -y install libgl1-mesa-dev libglu1-mesa-dev libxxf86dga-dev libxxf86vm-dev 
sudo apt -y install libx11-xcb-dev libxcb1-dev libcurl4-openssl-dev 
sudo apt -y install libopenal-dev libopus-dev libvorbis-dev libopusfile-dev libmad0-dev 
#
# opengl 
sudo apt -y install libglfw3-dev libgles2-mesa-dev mesa-utils libasound2-dev 
sudo apt -y install libglew-dev freeglut3 freeglut3-dev
#
# sdl
sudo apt -y install libsdl2-dev libsdl2-image-dev
#
# freetype
sudo apt -y install libfreetype6-dev libfreetype6
#
# mpv
sudo apt -y install mpv 
#
# samba
sudo apt -y install samba samba-common-bin smbclient libsmbclient-dev
#
# wayland
#sudo apt -y install libwayland-dev libxkbcommon-dev wayland-protocols extra-cmake-modules
#
# ffmpeg 
sudo apt -y install libfdk-aac-dev libx264-dev 
git clone -b master https://github.com/FFmpeg/FFmpeg
#
# github clones
cd ..
git clone -b master https://github.com/glfw/glfw
git clone -b master https://github.com/libsdl-org/SDL
git clone -b master https://github.com/ocornut/imgui
git clone https://github.com/colinpaint/paint
git clone https://github.com/colinpaint/myimgui
#
# quake 
#git clone https://github.com/Novum/vkQuake
#git clone https://github.com/kondrak/vkQuake2
#git clone https://github.com/suijingfeng/vkQuake3
#git clone https://github.com/drags/docker-quake2
#git clone https://github.com/nrempel/q3-server
#
OS=$(uname -m)
echo "$OS"
if [ "$OS" == "aarch64" ]; then
  echo "install raspberry pi"
  sudo apt -y install libvulkan1 mesa-vulkan-drivers
  #git clone https://github.com/colinporth/gpio  
  #git clone https://github.com/colinporth/opt
else 
  echo "install x86"
   # vulkan
  sudo apt -y install libvulkan1 mesa-vulkan-drivers vulkan-tools libvulkan-dev vulkan-validationlayers-dev spirv-tools
  #
  # enable ssh
  #sudo apt -y install openssh-server
  #sudo systemctl status ssh
  #sudo ufw allow ssh
  #  
  # mediaSDK
  #git clone -b master https://github.com/Intel-Media-SDK/MediaSDK
  #
  # vaapi
  #sudo apt -y install libva-dev vainfo
  #git clone -b master https://github.com/intel/libva
  #git clone -b master https://github.com/intel/libva-utils
  #sudo apt -y install libmfx-dev 
  #git clone -b master https://github.com/intel/gmmlib
  #git clone -b master https://github.com/intel/media-driver
  #git clone -b master https://github.com/intel/intel-vaapi-driver
  #git clone -b master https://github.com/mpv-player/mpv
  #git clone -b master https://gitlab.freedesktop.org/drm/igt-gpu-tools
fi
#
mkdir -p tv
chmod 0777 tv
#
cd linux
